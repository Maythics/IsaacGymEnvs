# used to create the object
name: ShadowPour

physics_engine: ${..physics_engine}

env:
  numEnvs: ${resolve_default:8192,${...num_envs}}
  envSpacing: 0.75
  episodeLength: 800
  enableDebugVis: False
  aggregateMode: 1

  clipObservations: 5.0
  clipActions: 1.0

  stiffnessScale: 1.0
  forceLimitScale: 1.0

  useRelativeControl: False
  dofSpeedScale: 20.0
  actionsMovingAverage: 1.0
  actionSpeedScale: 1.0
  controlFrequencyInv: 1

  startPositionNoise: 0.01
  startRotationNoise: 0.0

  resetPositionNoise: 0.01
  resetRotationNoise: 0.0
  resetDofPosRandomInterval: 0.2
  resetDofVelRandomInterval: 0.0

  forceScale: 0
  forceProbRange: [0.01, 0.1]
  forceDecay: 0.99
  forceDecayInterval: 0.08

  # Parent reward terms (only the action / dof-vel / object-vel penalties are
  # actually used by ShadowPour; dist/rot/reachGoal/fall scales are still read
  # by the parent constructor so values are kept here for compatibility).
  distRewardScale: -10.0
  rotRewardScale: 1.0
  rotEps: 0.1
  actionPenaltyScale: -0.0002
  reachGoalBonus: 0
  fallDistance: 0.35
  fallPenalty: -50.0

  objLinvelPenaltyScale: -0.005
  objAngvelPenaltyScale: -0.02
  dofVelPenaltyScale: -0.001
  palmDistPenaltyScale: 0.0
  objLinvelLimit: 0.6
  objAngvelLimit: 10.0
  dofVelLimit: 3.0

  # Object pool — forced to ["bottle"] inside the task class but kept for clarity
  objectType: "bottle"
  objectTypePool: ["bottle"]
  objectScaleRanges:
    bottle: [0.95, 1.05]
    default: [1.0, 1.0]
  observationType: "full_state"
  asymmetric_observations: False
  successTolerance: 0.20
  printNumSuccesses: False
  maxConsecutiveSuccesses: 0

  asset:
    assetFileName: "mjcf/open_ai_assets/hand/shadow_hand.xml"
    assetFileNameBlock: "urdf/objects/cube_multicolor.urdf"
    assetFileNameEgg: "mjcf/open_ai_assets/hand/egg.xml"
    assetFileNamePen: "mjcf/open_ai_assets/hand/pen.xml"
    assetFileNameBottle: "urdf/bottle/bottle_rigid.urdf"

  enableCameraSensors: False

  # ---- ShadowPour task parameters ----
  # Hand initial roll angle (radians). 80–100 deg by default.
  rollAngleMin: 1.396  # 80 deg
  rollAngleMax: 1.745  # 100 deg
  # ShadowHand at default MJCF orientation: fingers point world +Z, palm faces -Y.
  # Roll = rotation about the along-fingers axis = world Z.
  rollAxis: [0.0, 1.0, 0.0]

  # Sub-goal trajectory
  numSubGoals: 5
  pourFinalPitchDeg: 90.0
  pourPitchAxis: [0.0, 1.0, 0.0]   # world Y

  # Bottle spawn (palm-anchored). World-frame biases shift the bottle from
  # the palm into the grasp zone; tune empirically.
  bottleSpawnPosNoise: 0.02
  bottleSpawnXBias: 0.03
  bottleSpawnYBias: -0.08
  bottleSpawnZOffset: 0.10
  bottleSpawnRotNoise: 0.20

  # Phase 0: grasp shaping (negative scales → larger reward when distance shrinks)
  graspProximityScale: -5.0
  graspContactScale: -2.0
  graspBonus: 50.0
  graspProximityThreshold: 0.10
  graspLinvelThreshold: 0.20
  graspAngvelThreshold: 1.50

  # Phase 1+: sub-goal pursuit
  subgoalRotScale: 2.0
  subgoalPosScale: -1.0
  subgoalAdvanceBonus: 100.0
  subgoalSuccessTolerance: 0.20
  subgoalHoldFrames: 10
  finalSuccessBonus: 400.0

task:
  randomize: False
  randomization_params:
    frequency: 720
    observations:
      range: [0, .002]
      range_correlated: [0, .001]
      operation: "additive"
      distribution: "gaussian"
    actions:
      range: [0., .05]
      range_correlated: [0, .015]
      operation: "additive"
      distribution: "gaussian"
    sim_params:
      gravity:
        range: [0, 0.4]
        operation: "additive"
        distribution: "gaussian"
    actor_params:
      hand:
        color: True
        tendon_properties:
          damping:
            range: [0.3, 3.0]
            operation: "scaling"
            distribution: "loguniform"
          stiffness:
            range: [0.75, 1.5]
            operation: "scaling"
            distribution: "loguniform"
        dof_properties:
          damping:
            range: [0.3, 3.0]
            operation: "scaling"
            distribution: "loguniform"
          stiffness:
            range: [0.75, 1.5]
            operation: "scaling"
            distribution: "loguniform"
          lower:
            range: [0, 0.01]
            operation: "additive"
            distribution: "gaussian"
          upper:
            range: [0, 0.01]
            operation: "additive"
            distribution: "gaussian"
        rigid_body_properties:
          mass:
            range: [0.5, 1.5]
            operation: "scaling"
            distribution: "uniform"
            setup_only: True
        rigid_shape_properties:
          friction:
            num_buckets: 250
            range: [0.7, 1.3]
            operation: "scaling"
            distribution: "uniform"
      object:
        rigid_body_properties:
          mass:
            range: [0.5, 1.5]
            operation: "scaling"
            distribution: "uniform"
            setup_only: True
        rigid_shape_properties:
          friction:
            num_buckets: 250
            range: [0.7, 1.3]
            operation: "scaling"
            distribution: "uniform"

sim:
  dt: 0.01667
  substeps: 2
  up_axis: "z"
  use_gpu_pipeline: ${eq:${...pipeline},"gpu"}
  gravity: [0.0, 0.0, -9.81]
  physx:
    num_threads: ${....num_threads}
    solver_type: ${....solver_type}
    use_gpu: ${contains:"cuda",${....sim_device}}
    num_position_iterations: 8
    num_velocity_iterations: 0
    max_gpu_contact_pairs: 8388608
    num_subscenes: ${....num_subscenes}
    contact_offset: 0.002
    rest_offset: 0.0
    bounce_threshold_velocity: 0.2
    max_depenetration_velocity: 1000.0
    default_buffer_size_multiplier: 5.0
    contact_collection: 0
