# Uses the unchanged ShadowHand actor/critic interface so old checkpoints load.
defaults:
  - ShadowHandPPO
  - _self_

params:
  config:
    name: ${resolve_default:ShadowHandTrack,${....experiment}}
